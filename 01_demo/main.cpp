#include <iostream>

using namespace std;
class Dog
{
    //不写关键字的时候默认为private
public:
    string name;
    int age;
};
int main()
{
    //ctral + A 全选  ctral + i 自动对其
    //从栈中实例化对象

    //从堆重实例化对象 要用delete删除

    cout << "Hello World!" << endl;
    return 0;
}
